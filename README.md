# ngvm
Nygastic Virtual Machine

Guide:

- Download @ngvm.exe
- Make sure to store it where you can easily reach it
- Double click on it and your terminal opens

Now you can do for example:

- `ngvm --vm` -> `vm <name>`
  
